fastapi==0.115.6
uvicorn[standard]==0.32.1
# CPU-only torch — install before open-clip-torch to avoid pulling the CUDA wheel
# (done in Dockerfile via --index-url; listed here for reference)
torch
open-clip-torch==2.26.1
Pillow==11.0.0
httpx==0.28.1
pydantic==2.10.4
